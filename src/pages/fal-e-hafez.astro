---
import BaseLayout from '../layouts/BaseLayout.astro';
import FalHafezArticle from '../components/FalHafezArticle.astro';
import Footer from '../components/Footer.astro';
import { FAL_HAFEZ } from '../data/falHafez';
import { momentPageSchema } from '../lib/schema'
import { lastmodIso } from '../lib/sitemapLastmod.js';;

// The English Fal-e Hafez page. The Farsi pair is src/pages/fa/fal-e-hafez.astro.
//
// A static route rather than a dynamic one: there is exactly one of these, and
// getStaticPaths over a single-element array would be ceremony.
const path = '/fal-e-hafez/';

// The description leads with the definition, because the query behind this page
// is often literally "what is fal-e hafez". Long enough to be a real snippet,
// short enough not to be truncated.
// Shortened 2026-09-05 from 218 characters, the longest on the site. The
// definition earns its place because that is the query; the Yalda and Norooz
// sentence moves into the page body where it already appears.
const description =
  'A question put to a book: hold your question, open the Divan of Hafez at random, and read the ghazal you land on. Do it on iPhone in Persian or English.';
// "in English" added 2026-09-16, from measured Search Console demand rather
// than a guess. Over the 90 days to 16 September this page drew its traffic on
// `fal-e hafez english` (11 impressions, position 8.3), `faal hafiz` (5, 8.0),
// `faal e hafiz` (5, 10.8) and `fale hafez english` (1, 41.0): the word people
// type is "english", and the title did not contain it. Zero clicks on 33
// impressions at page-one positions. "of Hafez" comes out to pay for it, which
// costs nothing because "Hafez" is already the second word of the title.
// 60 characters exactly.
const title = 'Fal-e Hafez in English: the Divan, opened at random | Hamdam';

const jsonLd = momentPageSchema({
  dateModified: lastmodIso(Astro.url.pathname),
  lang: 'en',
  name: FAL_HAFEZ.titleEn,
  description,
  url: new URL(path, Astro.site).href,
  breadcrumbHome: 'Hamdam',
  homeUrl: new URL('/', Astro.site).href,
});
---

<BaseLayout
  title={title}
  description={description}
  lang="en"
  path={path}
  jsonLd={jsonLd}
  navOverDarkHero={true}
>
  <FalHafezArticle lang="en" />
  <Footer lang="en" />
</BaseLayout>
