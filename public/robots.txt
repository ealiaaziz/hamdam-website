# Hamdam. https://hamdam.com.au
#
# Policy, set 2026-08-07 by the site owner: every crawler is welcome, including
# the ones that build training corpora.
#
# This file has now been wrong in both directions. It began by blocking GPTBot,
# ChatGPT-User, CCBot, anthropic-ai and Claude-Web outright, which also blocked
# the user-triggered fetch an assistant makes when somebody asks it a question,
# so an assistant asked "what is a good Persian poetry app" could not read this
# site in order to answer. It was then narrowed to block training only. The
# owner's call is that discovery is worth more than the reservation: for an app
# this small, being absent from an answer is the expensive outcome, and being
# present in a training corpus is part of how a model comes to know the product
# exists at all.
#
# So there are no Disallow rules here, and no Content-Signal reservation. The
# argument for a narrower policy, and the exact agent-by-agent split it would
# need, is kept in docs/seo/2026-08-07-ai-crawler-policy.md rather than deleted
# from history, so reversing this is an edit rather than a rediscovery.
#
# This file used to warn that Cloudflare's managed AI crawler block sat in front
# of the origin and answered 403 before robots.txt was ever requested, making
# everything below inert. That warning was removed on 2026-09-16 after the check
# the policy doc prescribes was run and every agent came back 200. The result and
# its one caveat are recorded in that doc under "Verified 2026-09-16".

User-agent: *
Allow: /

Sitemap: https://hamdam.com.au/sitemap-index.xml
