# Hamdam Website — Progress

## Phase W1 — Cinematic warm-dawn bilingual relaunch (shipped 2026-07-13)

Full transformation of the landing experience ahead of the 2 August 2026 app
launch. Commits `c19efc8..3bdd35c` on `main`, tagged `web-w1-launch-ready`.

**Built:** warm-dawn brand tokens (`src/styles/tokens.css`) + per-locale font
preloads; pure locale resolver (`src/lib/locale.js`) driving the switcher and
hreflang from one source of truth; Persian byte-check pre-commit hook
(`scripts/check-persian.mjs`, zero-dep via core.hooksPath); scroll-driven
cinematic sunrise hero (`src/lib/cinematic.js` pure timeline + HeroCinematic,
night → first light → dawn → morning, deterministic 40-star field seed 42,
mid-dawn contrast scrim, full reduced-motion path, `?dawn=N` review pin);
gentle reveal system (`src/lib/reveal.js`); verse showcase fed byte-exact from
the iOS app's verse bank (hafez-016 hero, rumi-011, parvin-008) — fixing a
live verse misattributed to Hafez (it was Rumi, Masnavi I); OG images (EN+FA)
generated by `scripts/generate-og.mjs` (Chrome render + Sharp); webmanifest
icons from the real app icon; SoftwareApplication JSON-LD (prices deliberately
unpublished); Smart App Banner meta; official App Store badge gated behind
`APP_STORE.RELEASED = false` in `src/lib/appStore.js` (flip on launch day);
Cloudflare Web Analytics unblocked in CSP (Option B, no custom events); HSTS
31536000 + preload; immutable caching for hashed assets; a11y pass (focus
rings, 44px targets, footer contrast, label-in-name fix).

**Test suite:** 60 Vitest cases across locale/cinematic/reveal/appStore.

**Verification matrix (Phase 11):**

| Row | Result |
|---|---|
| Lighthouse local (mobile): `/` 98/100/100/100, `/fa/` 97/100/100/100 | PASS (targets 95+/100/100/100) |
| CLS | 0 (target < 0.05) |
| Switcher round-trip preserves path (all page pairs) | PASS |
| `npm run check:persian` | PASS (pre-commit gated) |
| Hero text contrast at all gradient phases | PASS (scrim design; screenshots at 0/0.35/0.6/0.78/1) |
| iPhone Safari `/` and `/fa/` real-device pass | PENDING — Ealia |
| Safari macOS Reduce Motion | PENDING — Ealia (logic unit-tested) |
| VoiceOver language voices | PENDING — Ealia |
| Smart App Banner visible on iPhone | N/A until app is live (2 Aug) |

**CSP note:** already enforcing before W1 (not Report-Only); root cause of the
pre-W1 live violations (Astro-inlined styles/scripts) fixed at build level
(`inlineStylesheets: 'never'`, `assetsInlineLimit: 0`). Post-deploy check:
zero console CSP violations, analytics pageviews flowing.

**Launch-day flips (2 Aug):** set `APP_STORE.RELEASED = true`; supply Ealia's
Persian aria-label for the download badge (currently English on both locales).

**Deferred to W2:** verse rotation controls, blog/newsletter, PWA shell,
`/fa` copy re-authoring by a native speaker (MT text ships undisclosed per
Ealia's 2026-07-13 call), FA legal-page localisation.
